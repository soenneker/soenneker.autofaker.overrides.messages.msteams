name: build-and-test
on:
  # Testing - run for any PRs.
  pull_request:
    branches:
      - main

env:
    "PipelineEnvironment": true

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore tests
        run: dotnet restore test/Soenneker.AutoFaker.Overrides.Messages.MsTeams.Tests/Soenneker.AutoFaker.Overrides.Messages.MsTeams.Tests.csproj --verbosity minimal

      - name: Build tests
        run: dotnet build test/Soenneker.AutoFaker.Overrides.Messages.MsTeams.Tests/Soenneker.AutoFaker.Overrides.Messages.MsTeams.Tests.csproj --configuration Release --no-restore --verbosity minimal

      - name: Test
        run: dotnet test --project test/Soenneker.AutoFaker.Overrides.Messages.MsTeams.Tests/Soenneker.AutoFaker.Overrides.Messages.MsTeams.Tests.csproj --configuration Release --no-build --no-restore --verbosity normal

      - name: Restore library
        run: dotnet restore src/Soenneker.AutoFaker.Overrides.Messages.MsTeams/Soenneker.AutoFaker.Overrides.Messages.MsTeams.csproj --verbosity minimal

      - name: Build library
        run: dotnet build src/Soenneker.AutoFaker.Overrides.Messages.MsTeams/Soenneker.AutoFaker.Overrides.Messages.MsTeams.csproj --configuration Release --no-restore --verbosity minimal

      - name: Pack
        shell: bash
        run: |
          mkdir -p artifacts
          dotnet pack src/Soenneker.AutoFaker.Overrides.Messages.MsTeams/Soenneker.AutoFaker.Overrides.Messages.MsTeams.csproj \
            --configuration Release \
            --no-build \
            --no-restore \
            --output ./artifacts
